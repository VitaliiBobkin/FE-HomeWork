# FE-HomeWork

## Home_Work_21
**implementation of the padString function, which takes four arguments**

This project contains the implementation of the `padString` function in JavaScript.

## Function Purpose

The `padString` function adjusts the length of a given string by adding a specified character either to the left or right.
It ensures input validation and returns informative errors when inputs are incorrect.

